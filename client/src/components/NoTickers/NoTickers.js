import React from 'react';

import './NoTickers.css';

export const NoTickers = () => {
  return (
    <section className='no-tickers'>
      <h2>No tickers here now!</h2>
      <h2>You can retrieve them from the left sidebar.</h2>
    </section>
  );
};
